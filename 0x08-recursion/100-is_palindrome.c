#include "main.h"
#include <string.h>
/**
 * is_palindrome - Checks if a string is a palindrome
 * @s: string to be checked
 * Return: 0 if string is not palindrome else 1
 */
int is_palindrome(char *s)
{

	if (strlen(s) == 0)
	{
		return (0);
	}
	else
	{
		return (1);
	}
	return (0);
}
